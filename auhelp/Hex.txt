
Hex

¬озвращает строковое представление целого числа или бинарных данных, преобразованных к шестнадцатеричному виду.

Hex ( expression [, length] )

---  “аблица параметров  ---
expression
	¬ыражение дл€ преобразовани€.
length
	[опционально] число возвращаемых символов (до 8) дл€ целого числа.
	—имволы обрезаютс€ с левой стороны, если указанное значение этого параметра слишком мало.
	Ётот параметр игнорируетс€, если выражение €вл€етс€ бинарными данными.

---  “аблица возвращаемых значений  ---
”спех:	¬озвращает строку, длина которой дополнена нул€ми до указанного количество символов (параметр length), если это необходимо дл€ целого числа.
	¬озвращает преобразованное в бинарный тип.
—пециально:	¬озвращает "" (пустую строку), если длина меньше 1.
ќшибка:	¬озвращает "" (пустую строку) и устанавливает значение @error равным 1, если длина больше 8.

---  ѕримечани€  ---
‘ункци€ работает только с 32-битными знаковыми целыми числами (от -2147483648 до 2147483647)

‘ункци€ также устанавливает @error = 1, если указано недостаточное количество символов в параметре length, при этом возвращаетс€ строка состо€ща€ из младших разр€дов (обрезаетс€ старшие разр€ды слева).

---  —мотрите также  ---
Dec, BitAND, BitNOT, BitOR, BitRotate, BitShift, BitXOR