
StringSplit

–аздел€ет строку на подстроки, взависимости от заданного разделител€.

StringSplit ( "string", "delimiters" [, flag] )

---  “аблица параметров  ---
string
	»спользуема€ строка.
delimiters
	ќдин или более символов используютс€ как разделитель (с учетом регистра).
flag
	[опционально] изменение способа разделени€, совместное использование флагов:
	  flag = 0 (по умолчанию), каждый символ в разделителе строки €вл€етс€ разделителем.
	  flag = 1, вс€ строка разделител€ €вл€етс€ разделителем.
	  flag = 2, отключить возвращение количества элементов в первый элемент массива (необходимо использовать UBound () чтобы получить размерность массива).

---  “аблица возвращаемых значений  ---
¬озвращает массив, в котором по умолчанию первый элемент $array[0] содержит количество возвращЄнных строк, остальные элементы ($array[1], $array[2], и т.д.) содержат разделЄнные строки.  ≈сли флаг = 2, то в первый элемент не возвращаетс€ количество элементов, а возвращаетс€ первый элемент разделЄнной строки.

≈сли разделитель в искомой строке не найден, то @error = 1, количество в $array[0]=1 и вс€ строка возвращаетс€ в $array[1].

---  ѕримечани€  ---
≈сли вы используете пустую строку как разделитель "", то каждый символ возвратитс€ в элемент массива.

≈сли вы хотите использовать подстроку как разделитель, а не как одиночные символы, смотрите пример ниже.

<!--StringSplit очень полезна как альтернатива StringInStr и как средство заполнени€ массива.

ѕредупреждение, если вы в разделителе используете макро - @CRLF, то фактически используетс€ 2 строчных символа, которые сгенерируют дополнительные пустые элементы в массиве.-->

---  —мотрите также  ---
StringInStr, StringLeft, StringLen, StringLower, StringMid, StringRight, StringTrimLeft, StringTrimRight, StringUpper