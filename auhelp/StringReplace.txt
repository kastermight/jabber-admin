
StringReplace

«амен€ет фрагмент в строке.

StringReplace ( "string", "searchstring/start", "replacestring" [, occurrence [, casesense]] )

---  “аблица параметров  ---
string
	»спользуема€ строка.
searchstring/start
	—трока поиска или позици€ символа начала замены.
replacestring
	—трока замены.
occurrence
	[опционально] количество выполн€емых замен. »спользуйте отрицательное число, чтобы выполнить замену справа.
	0 = выполнить замену всех найденных строк (по умолчанию)
casesense
	[опционально] флаг установки чувствительности к регистру написани€.
	0 = не учитывать регистр, используетс€ локальный €зык (по умолчанию)
	1 = учитывать регистр
	2 = не учитывать регистр, используетс€ основное / быстрое сравнение

---  “аблица возвращаемых значений  ---
¬озвращает новую строку, в @extended содержитс€ число замен.

---  ѕримечани€  ---
ѕо умолчанию или при положительном occurrence поиск/замена производитс€ слева направо.  “аким образом, StringReplace("aaa", "aa", "bb") возвращает "bba"

≈сли используетс€ метод замены в указанной позиции, то параметры occurrence и casesense игнорируютс€. ≈сли замена строки не может быть сохранена, то возвращаетс€ пуста€ строка и @error устанавливаетс€ равным 1.

---  —мотрите также  ---
StringLeft, StringLen, StringLower, StringMid, StringRight, StringTrimLeft, StringTrimRight, StringUpper, StringStripWS, StringAddCR