
GUICtrlCreateCombo

Создаёт ComboBox в GUI.

GUICtrlCreateCombo ( "text", left, top [, width [, height [, style [, exStyle]]]] )

---  Таблица параметров  ---
text
	Текст, который первым будет отображаться в выпадающем списке.
left
	Координата левого края. Если -1 тогда координата в соответствии с GUICoordMode.
top
	Координата верхнего края. Если -1 тогда координата в соответствии с GUICoordMode.
width
	[опционально] Ширина элемента (по умолчанию предыдущее значение).
height
	[опционально] Высота элемента (по умолчанию предыдущее значение).
style
	[опционально] Стиль элемента. Смотрите Стили элементов GUI в приложении.
	
	по умолчанию (-1) : $CBS_DROPDOWN, $CBS_AUTOHSCROLL, $WS_VSCROLL
	принудительный стиль : $WS_TABSTOP
exStyle
	[опционально] Расширенный стиль элемента. Смотрите таблицу расширенных стилей.

---  Таблица возвращаемых значений  ---
Успех:	Возвращает идентификатор (controlID) нового элемента.
Ошибка:	Возвращает 0.

---  Примечания  ---
Для получения параметров элемента смотрите GUICtrlRead.
Чтобы установить или изменить параметры элемента смотрите GUICtrlUpdate....

Под Windows XP/2003 будет автоматически установлен размер открытого combo. На других версиях Windows вы можете определить этот размер параметром "высота", если по умолчанию высота не достаточна, чтобы вместить по меньшей мере одну строку.

Чтобы объединить стили со стилем по умолчанию используйте BitOr($GUI_SS_DEFAULT_COMBO, новый стиль,...).

Чтобы использовать параметры, указанные выше, вы должны включить в ваш скрипт #include <ComboConstants.au3>.

По умолчанию стиль изменения размеров $GUI_DOCKHEIGHT (не изменяется высота).

---  Смотрите также  ---
GUICoordMode (Option), GUICtrlSetData, GUICtrlUpdate..., GUIGetMsg